#include <stdio.h>
main(){
	int a,b;
	printf("Enter two Integers in the machine to get their sum,\nEnter 1st Integer: ");
	scanf("%d",&a);
	printf("Enter 2nd Number: ");
	scanf("%d",&b);
	printf("Sum of both numbers = %d",a+b);
}